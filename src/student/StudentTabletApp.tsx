import { useEffect, useState } from 'react';

interface StudentProfile { name: string; standard: string; coachingType: string; roomNumber: string; wingNumber: string; tabletId?: string; }
interface ActiveSession { sessionToken: string; studentId: string; studentName: string; tabletId: string; startedAt: string; student?: StudentProfile; }
const SESSION_KEY = 'teb_student_session_token';
function normalizePin(value: string) { return value.trim().replace(/^PIN-/i, '').replace(/\D/g, '').slice(0, 20); }
function generatePin() { return String(Math.floor(Math.random() * 10000)).padStart(4, '0'); }
function formatElapsed(startedAt: string) { const seconds = Math.max(0, Math.floor((Date.now() - new Date(startedAt).getTime()) / 1000)); const h = String(Math.floor(seconds / 3600)).padStart(2, '0'); const m = String(Math.floor((seconds % 3600) / 60)).padStart(2, '0'); const s = String(seconds % 60).padStart(2, '0'); return `${h}:${m}:${s}`; }

export function StudentTabletApp() {
  const [mode, setMode] = useState<'login' | 'register'>('login');
  const [name, setName] = useState(''); const [pin, setPin] = useState('');
  const [standard, setStandard] = useState('Std 8'); const [coachingType, setCoachingType] = useState('Coaching'); const [roomNumber, setRoomNumber] = useState(''); const [wingNumber, setWingNumber] = useState('');
  const [generatedPin, setGeneratedPin] = useState('');
  const [session, setSession] = useState<ActiveSession | null>(null); const [elapsed, setElapsed] = useState('00:00:00'); const [loading, setLoading] = useState(true); const [message, setMessage] = useState(''); const [error, setError] = useState('');
  const student = session?.student;

  useEffect(() => { let cancelled = false; async function restore() { const token = localStorage.getItem(SESSION_KEY); if (!token) { if (!cancelled) setLoading(false); return; } try { const response = await fetch('/api/student/session', { headers: { Authorization: `Bearer ${token}`, Accept: 'application/json' } }); const result = await response.json().catch(() => ({})); if (!response.ok || !result?.session) throw new Error(result?.error || 'Session expired. Please login again.'); if (!cancelled) { setSession(result.session); setMessage('Active session restored.'); } } catch (e) { localStorage.removeItem(SESSION_KEY); if (!cancelled) setError(e instanceof Error ? e.message : 'Session could not be restored.'); } finally { if (!cancelled) setLoading(false); } } restore(); return () => { cancelled = true; }; }, []);
  useEffect(() => { if (!session) return; setElapsed(formatElapsed(session.startedAt)); const timer = window.setInterval(() => setElapsed(formatElapsed(session.startedAt)), 1000); return () => window.clearInterval(timer); }, [session]);

  async function login() { setError(''); setMessage(''); const cleanName = name.trim(); const cleanPin = normalizePin(pin); if (!cleanName) { setError('Please enter Student Name.'); return; } if (!cleanPin) { setError('Please enter Student PIN.'); return; } setLoading(true); try { const response = await fetch('/api/student/activate', { method: 'POST', headers: { 'Content-Type': 'application/json', Accept: 'application/json' }, body: JSON.stringify({ name: cleanName, pin: cleanPin }) }); const result = await response.json().catch(() => ({})); if (!response.ok) throw new Error(result?.error || `Login failed (${response.status}).`); if (!result?.session) throw new Error('Login response is invalid.'); localStorage.setItem(SESSION_KEY, result.session.sessionToken); setSession(result.session); setName(''); setPin(''); setMessage('Student signed in successfully.'); } catch (e) { setError(e instanceof Error ? e.message : 'Login failed.'); } finally { setLoading(false); } }

  async function registerStudent() { setError(''); setMessage(''); setGeneratedPin(''); const cleanName = name.trim(); const cleanRoom = roomNumber.trim(); const cleanWing = wingNumber.trim(); if (!cleanName) { setError('Please enter Student Name.'); return; } if (!cleanRoom || !cleanWing) { setError('Please enter Room Number and Wing Number.'); return; } setLoading(true); try { let response: Response | null = null; let result: any = {}; let appPin = ''; for (let attempt = 0; attempt < 5; attempt++) { appPin = generatePin(); response = await fetch('/api/student/register', { method: 'POST', headers: { 'Content-Type': 'application/json', Accept: 'application/json' }, body: JSON.stringify({ name: cleanName, pin: appPin, standard, coachingType, roomNumber: cleanRoom, wingNumber: cleanWing }) }); result = await response.json().catch(() => ({})); if (response.ok) break; if (response.status !== 409) throw new Error(result?.error || `Registration failed (${response.status}).`); } if (!response?.ok) throw new Error(result?.error || 'Registration failed.'); setGeneratedPin(appPin); setMessage('Registration successful. Save this App PIN for future login.'); setName(''); setPin(''); setRoomNumber(''); setWingNumber(''); } catch (e) { setError(e instanceof Error ? e.message : 'Registration failed.'); } finally { setLoading(false); } }

  async function returnTablet() { if (!session) return; setError(''); setMessage(''); setLoading(true); try { const response = await fetch('/api/student/return', { method: 'POST', headers: { 'Content-Type': 'application/json', Accept: 'application/json' }, body: JSON.stringify({ sessionToken: session.sessionToken }) }); const result = await response.json().catch(() => ({})); if (!response.ok) throw new Error(result?.error || 'Could not return the tablet.'); localStorage.removeItem(SESSION_KEY); setSession(null); setElapsed('00:00:00'); setName(''); setPin(''); setMessage(`Tablet checked out. Session duration: ${result.durationMinutes} minute(s).`); } catch (e) { setError(e instanceof Error ? e.message : 'Could not return the tablet.'); } finally { setLoading(false); } }

  if (loading && !session && !error) return <main className="min-h-screen bg-slate-50 flex items-center justify-center p-5 text-slate-900"><section className="w-full max-w-md rounded-3xl bg-white shadow-xl border border-slate-200 p-7 text-center"><div className="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-2xl bg-indigo-600 text-2xl font-bold text-white">TEB</div><p className="font-semibold">Checking student session...</p></section></main>;
  return <main className="min-h-screen bg-slate-50 flex items-center justify-center p-5 text-slate-900"><section className="w-full max-w-md rounded-3xl bg-white shadow-xl border border-slate-200 p-7"><div className="text-center mb-7"><div className="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-2xl bg-indigo-600 text-2xl font-bold text-white">TEB</div><h1 className="text-2xl font-bold">Student App</h1><p className="mt-1 text-sm text-slate-500">Student registration, secure login and attendance</p></div>
    {!session ? <><div className="flex mb-6 rounded-xl bg-slate-100 p-1"><button type="button" onClick={() => { setMode('login'); setError(''); setMessage(''); setGeneratedPin(''); }} className={`flex-1 rounded-lg px-3 py-3 text-sm font-semibold ${mode === 'login' ? 'bg-white shadow text-slate-900' : 'text-slate-500'}`}>Existing Student</button><button type="button" onClick={() => { setMode('register'); setError(''); setMessage(''); setGeneratedPin(''); }} className={`flex-1 rounded-lg px-3 py-3 text-sm font-semibold ${mode === 'register' ? 'bg-white shadow text-slate-900' : 'text-slate-500'}`}>First Time</button></div>
      {mode === 'login' ? <div className="space-y-4"><label className="block"><span className="mb-1.5 block text-sm font-medium">Student Name</span><input value={name} onChange={e => setName(e.target.value)} onKeyDown={e => { if (e.key === 'Enter') login(); }} placeholder="Enter student name" className="w-full rounded-xl border border-slate-300 px-4 py-3 outline-none focus:border-indigo-500" autoFocus /></label><label className="block"><span className="mb-1.5 block text-sm font-medium">Student PIN</span><input value={pin} maxLength={20} onChange={e => setPin(normalizePin(e.target.value))} onKeyDown={e => { if (e.key === 'Enter') login(); }} inputMode="numeric" pattern="[0-9]*" type="password" placeholder="Enter student PIN" className="w-full rounded-xl border border-slate-300 px-4 py-3 outline-none focus:border-indigo-500" /></label><button type="button" disabled={loading || !name.trim() || !pin.trim()} onClick={login} className="w-full rounded-xl bg-indigo-600 px-4 py-3.5 font-semibold text-white disabled:opacity-60">{loading ? 'Signing in...' : 'Login & Start'}</button><p className="text-center text-xs text-slate-500">Enter Student Name and App PIN. Tablet is assigned by Admin.</p></div> : <div className="space-y-4"><label className="block"><span className="mb-1.5 block text-sm font-medium">Student Name</span><input value={name} onChange={e => setName(e.target.value)} placeholder="Enter student name" className="w-full rounded-xl border border-slate-300 px-4 py-3" autoFocus /></label><div className="grid grid-cols-2 gap-3"><select value={standard} onChange={e => setStandard(e.target.value)} className="w-full rounded-xl border border-slate-300 px-4 py-3"><option>Std 8</option><option>Std 9</option><option>Std 10</option><option>Std 11</option><option>Std 12</option></select><select value={coachingType} onChange={e => setCoachingType(e.target.value)} className="w-full rounded-xl border border-slate-300 px-4 py-3"><option>Coaching</option><option>Non-Coaching</option></select></div><div className="grid grid-cols-2 gap-3"><input value={roomNumber} onChange={e => setRoomNumber(e.target.value)} placeholder="Room Number" className="w-full rounded-xl border border-slate-300 px-4 py-3" /><input value={wingNumber} onChange={e => setWingNumber(e.target.value)} placeholder="Wing Number" className="w-full rounded-xl border border-slate-300 px-4 py-3" /></div><button type="button" disabled={loading || !name.trim() || !roomNumber.trim() || !wingNumber.trim()} onClick={registerStudent} className="w-full rounded-xl bg-indigo-600 px-4 py-3.5 font-semibold text-white disabled:opacity-60">{loading ? 'Registering...' : 'Register Student'}</button>{generatedPin && <div className="rounded-2xl bg-emerald-50 border border-emerald-200 p-5 text-center"><p className="text-sm font-semibold text-emerald-700">Registration Successful</p><p className="text-xs text-emerald-600 mt-1">Your App PIN</p><p className="text-4xl font-mono font-bold tracking-[0.35em] text-emerald-900 mt-2">{generatedPin}</p><p className="text-xs text-emerald-700 mt-3">Keep this PIN safe. Admin will assign your tablet before login.</p></div>}<p className="text-center text-xs text-slate-500">First-time students can register here. The App PIN is generated automatically.</p></div>}
    </> : <div className="space-y-5"><div className="rounded-2xl bg-emerald-50 p-5 text-center"><p className="text-sm font-medium text-emerald-700">Session Active • IN</p><p className="mt-1 text-xl font-bold">{session.studentName}</p><p className="mt-1 text-sm text-emerald-700">Assigned Tablet: {session.tabletId}</p><p className="mt-4 text-3xl font-mono font-bold text-emerald-900">{elapsed}</p></div>{student && <div className="grid grid-cols-2 gap-2 text-sm"><div className="rounded-xl bg-slate-50 p-3"><span className="text-slate-500">Standard</span><p className="font-semibold">{student.standard}</p></div><div className="rounded-xl bg-slate-50 p-3"><span className="text-slate-500">Type</span><p className="font-semibold">{student.coachingType}</p></div><div className="rounded-xl bg-slate-50 p-3"><span className="text-slate-500">Room</span><p className="font-semibold">{student.roomNumber}</p></div><div className="rounded-xl bg-slate-50 p-3"><span className="text-slate-500">Wing</span><p className="font-semibold">{student.wingNumber}</p></div></div>}<button type="button" disabled={loading} onClick={returnTablet} className="w-full rounded-xl bg-slate-900 px-4 py-3.5 font-semibold text-white disabled:opacity-60">{loading ? 'Checking out...' : 'CHECKOUT / RETURN TABLET'}</button></div>}
    {message && <p className="mt-5 rounded-xl bg-blue-50 px-4 py-3 text-sm text-blue-700">{message}</p>}{error && <p className="mt-5 rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}<p className="mt-6 text-center text-xs text-slate-400">TEB Management • Student access only</p></section></main>;
}
